# Credenciais — NUNCA versionar (formulário de contato e painel do blog)
bocchi-smtp.php
bocchi-oauth.php
*.secret.php

# Dependências / build
/vendor/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/

# diagnóstico temporário (apagar após uso)
smtp-diag*.php

# Dependências do gerador do blog (instaladas no CI)
/node_modules/

# Saída do empacotador (gerada localmente, não versionada)
/dist/
/deploy.zip
